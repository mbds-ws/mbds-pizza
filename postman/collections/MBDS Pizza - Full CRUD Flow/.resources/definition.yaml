$kind: collection
description: "End-to-end flow: login, categories CRUD, ingredients CRUD, pizzas
  CRUD, orders flow, then cleanup. Safe to run from the Postman Collection
  Runner."
auth:
  - id: bd6c0ab4-f107-40b5-ae66-243acf773726
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
