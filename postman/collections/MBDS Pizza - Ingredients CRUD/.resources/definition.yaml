$kind: collection
description: CRUD requests for ingredients. Run the auth collection first so
  {{token}} is available.
auth:
  - id: a61eee92-8db3-4d11-b9a5-0e0deb1ba9ed
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
