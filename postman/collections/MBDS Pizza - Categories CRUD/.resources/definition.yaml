$kind: collection
description: CRUD requests for categories. Run the auth collection first so
  {{token}} is available.
auth:
  - id: d4056fba-07f4-4618-bd5d-eb0dd8ef6f4e
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
