$kind: collection
description: CRUD requests for categories. Run the auth collection first so
  {{token}} is available.
auth:
  - id: f539e989-5854-4a9c-8ae0-79546d50c77f
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
