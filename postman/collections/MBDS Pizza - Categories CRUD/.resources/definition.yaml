$kind: collection
description: CRUD requests for categories. Run the auth collection first so
  {{token}} is available.
auth:
  - id: 4d8a77e8-1e21-46ca-b093-9b37662b7043
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
