$kind: http-request
url: "{{baseUrl}}/api/pizzas/{{pizzaId}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "Margherita Updated",
      "description": "Pizza classique mise à jour",
      "price": 28000,
      "available": true,
      "categoryId": {{categoryId}},
      "ingredientIds": [{{ingredientId}}]
    }
order: 11000
