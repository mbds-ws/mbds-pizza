$kind: collection
description: CRUD requests for ingredients. Run the auth collection first so
  {{token}} is available.
auth:
  - id: d982be30-de5e-4964-ae53-480432a6f86e
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
