$kind: collection
description: CRUD requests for ingredients. Run the auth collection first so
  {{token}} is available.
auth:
  - id: adbcef75-c83b-4574-b72d-ef48d5203b79
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
