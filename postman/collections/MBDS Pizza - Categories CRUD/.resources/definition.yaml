$kind: collection
description: CRUD requests for categories. Run the auth collection first so
  {{token}} is available.
auth:
  - id: 495c36f6-2556-4545-a64e-08b1b2243fca
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
