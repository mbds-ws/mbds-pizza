$kind: collection
description: "End-to-end flow: login, categories CRUD, ingredients CRUD, pizzas
  CRUD, then cleanup. Safe to run from the Postman Collection Runner."
auth:
  - id: 0ad030a6-eaed-45c2-be8e-2c1575f85b6d
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
